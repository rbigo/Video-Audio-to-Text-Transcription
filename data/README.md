# Local data directory

This directory is for local runtime data only. Git keeps only documentation and cookie placeholders.

Ignored runtime contents include downloaded audio, subtitles, transcripts, jobs, logs, and temp files.
